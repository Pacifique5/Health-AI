services:
  - type: web
    name: symptom-ai-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && gunicorn api_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
